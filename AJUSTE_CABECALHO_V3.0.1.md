# Ajuste do cabeçalho — v3.0.1

O seletor de espaço financeiro foi removido do cabeçalho e reposicionado no rodapé da barra lateral, junto do perfil.

## Resultado

- O título da página voltou a ter prioridade.
- Busca, sincronização, tema e nova movimentação continuam no cabeçalho.
- A troca entre espaços pessoais e compartilhados permanece disponível.
- No celular, o seletor aparece dentro do menu lateral.
- Nenhum dado, regra de permissão ou integração com o Supabase foi alterado.

## Atualização

Substitua `index.html`, `css/style.css`, `css/responsive.css` e `service-worker.js`. Não é necessário executar SQL novamente.
